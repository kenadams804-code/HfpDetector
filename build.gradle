buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // 同步升级为完美兼容高版本编译环境的现代核心插件
        classpath 'com.android.tools.build:gradle:8.5.0'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.20"
    }
}
